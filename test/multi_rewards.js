const MultiRewards = artifacts.require("MultiRewards");

contract("Multi Rewards", function(accounts) {
  let contract

  before(async () => {
    contract = await MultiRewards.deployed()
  })

  it("user should be able to stake");

  it("user should be able to stake");

  it("user should be able to stake");

  it("user should be able to stake");
});
